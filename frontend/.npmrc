# Keeps npm's cache inside the repo. This is needed in sandboxed environments
# where $HOME is not writable (npm otherwise fails to create ~/.npm/_logs), and
# is harmless elsewhere. Paths are relative to this directory.
cache=../.cache/npm
fund=false
audit=false
update-notifier=false
