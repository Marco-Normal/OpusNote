# Development and test dependencies (not needed to run the app).
-r requirements.txt
pytest>=8.0
httpx>=0.27
playwright>=1.45
# The guardrail's machinery — see docs/TEST-STRATEGY.md §5. Coverage reports only; the
# mutation score is a report until its baseline is known; hypothesis carries Slice 2.
coverage>=7.16
pytest-timeout>=2.3
hypothesis>=6.100
mutmut>=3.2
