# Runtime dependencies for the Opus Note API.
# Install into a virtual environment:
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
music21>=9.1
python-multipart>=0.0.9
